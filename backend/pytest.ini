[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    unit: Unit tests (fast, no external dependencies)
    integration: Integration tests (may need mocked services)
    slow: Slow tests (network, AI API calls)
